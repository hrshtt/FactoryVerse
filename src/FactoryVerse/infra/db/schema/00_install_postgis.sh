#!/bin/bash
# This script is not needed for PostgreSQL 16 with PostGIS
# PostGIS extensions will be installed via SQL commands in the template setup
echo "PostGIS will be installed via SQL commands in the template setup"
