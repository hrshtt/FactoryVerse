"""tests/live — contract tests against a running Factorio instance.

Every test here is marked `live` (registered in pytest.ini) and must SKIP
when no instance is reachable — a real skip, never a vacuous pass.
"""
